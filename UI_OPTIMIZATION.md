# 晨翼Agent UI 优化方案

## 📊 当前 UI 功能分析

### ✅ 已有功能
1. **消息列表** - 显示对话历史
2. **输入框** - 发送消息
3. **设置对话框** - 配置 API Key/Endpoint/Model
4. **工具对话框** - 手动执行工具（仅截图实现）
5. **状态栏** - 显示当前状态

### ❌ 缺失功能

#### 高优先级
1. **会话管理** - 无法新建/切换/删除会话
2. **消息操作** - 无法复制/删除/重新发送消息
3. **工具执行反馈** - 看不到工具执行过程
4. **权限状态** - 不清楚无障碍服务/截图权限状态
5. **历史记录** - 重启后消息丢失

#### 中优先级
6. **快捷指令** - 没有常用操作快捷按钮
7. **工具参数输入** - 工具对话框大部分是 TODO
8. **错误详情** - 错误信息不够详细
9. **加载动画** - 工具执行时无进度提示
10. **语音输入** - 没有语音识别功能

#### 低优先级
11. **主题切换** - 没有深色模式
12. **字体大小** - 无法调整
13. **消息搜索** - 无法搜索历史消息
14. **导出对话** - 无法导出聊天记录
15. **多语言** - 没有国际化支持

## 🎨 UI 优化方案

### 方案 1: 会话管理（最高优先级）

```
┌────────────────────────────────────┐
│  晨翼 Agent ●          [📝] [⚙️]  │  ← TopAppBar
├────────────────────────────────────┤
│  ┌────────────────────────────┐    │
│  │ 📱 会话列表                │    │  ← 左侧抽屉
│  │ ├─ 新建会话        [+]     │    │
│  │ ├─ 抖音自动化      12:30   │    │
│  │ ├─ 微信发消息      昨天    │    │
│  │ └─ 测试对话        3天前   │    │
│  └────────────────────────────┘    │
├────────────────────────────────────┤
│  当前会话: 抖音自动化              │
│  ┌──────────────────────────────┐  │
│  │ 状态: ✅ 内核就绪             │  │  ← 状态栏
│  │ 权限: ✅ 无障碍 ✅ 截图       │  │
│  └──────────────────────────────┘  │
├────────────────────────────────────┤
│  消息列表...                       │
└────────────────────────────────────┘
```

**实现要点**:
- 使用 `DrawerLayout` 或 `ModalNavigationDrawer`
- 会话数据持久化到 SQLite
- 支持会话重命名、删除、导出

### 方案 2: 消息操作

```
用户消息卡片:
┌────────────────────────────────────┐
│  👤 用户                    12:30  │
│  帮我打开抖音                      │
│  ┌────────────────────────────┐    │
│  │ 📋 复制  🔄 重发  🗑️ 删除  │    │  ← 长按显示菜单
│  └────────────────────────────┘    │
└────────────────────────────────────┘

助手消息卡片:
┌────────────────────────────────────┐
│  🤖 助手                    12:30  │
│  好的，正在打开抖音...             │
│  ├─ 🔧 执行工具: openApp          │  ← 工具执行记录
│  │   └─ ✅ 成功打开               │
│  └─ ✅ 完成                       │
│  ┌────────────────────────────┐    │
│  │ 📋 复制  🔊 朗读  🔄 重新生成 │    │
│  └────────────────────────────┘    │
└────────────────────────────────────┘
```

**实现要点**:
- 长按消息显示操作菜单
- 工具执行过程可视化
- 支持复制、重发、删除

### 方案 3: 权限状态面板

```
状态栏改进:
┌────────────────────────────────────┐
│  系统状态                          │
│  ├─ 🟢 内核: 已初始化              │
│  ├─ 🟢 无障碍服务: 已开启          │
│  ├─ 🟡 截图权限: 未授权 [授权]     │
│  └─ 🟢 OCR: 就绪                   │
│                                    │
│  快捷操作                          │
│  ├─ [📸 截图] [🔍 OCR] [📱 应用列表]│
│  └─ [⚙️ 设置] [❓ 帮助]            │
└────────────────────────────────────┘
```

**实现要点**:
- 实时显示权限状态
- 一键授权按钮
- 快捷工具按钮

### 方案 4: 工具执行可视化

```
工具执行过程:
┌────────────────────────────────────┐
│  🔄 正在执行工具...                │
│                                    │
│  第 1/3 轮                         │
│  ├─ 🔧 openApp("抖音")             │
│  │   └─ ⏳ 正在打开...             │
│  │                                 │
│  第 2/3 轮                         │
│  ├─ 🔧 tap(610, 2550)              │
│  │   └─ ✅ 点击成功                │
│  │                                 │
│  第 3/3 轮                         │
│  ├─ 🔧 ocr()                       │
│  │   └─ ⏳ 识别中...               │
│  │                                 │
│  [取消]                            │
└────────────────────────────────────┘
```

**实现要点**:
- 实时显示工具执行进度
- 支持取消操作
- 显示每一步的结果

### 方案 5: 快捷指令面板

```
快捷指令:
┌────────────────────────────────────┐
│  📱 常用操作                       │
│  ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 打开 │ │ 关闭 │ │ 截图 │       │
│  │ 抖音 │ │ 应用 │ │      │       │
│  └──────┘ └──────┘ └──────┘       │
│  ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ OCR  │ │ 点击 │ │ 滑动 │       │
│  │      │ │ 坐标 │ │      │       │
│  └──────┘ └──────┘ └──────┘       │
│                                    │
│  🎯 快捷任务                       │
│  ├─ 抖音点赞第一个视频             │
│  ├─ 微信发送消息                   │
│  └─ 浏览器搜索                     │
└────────────────────────────────────┘
```

**实现要点**:
- 预设常用操作
- 支持自定义快捷任务
- 一键执行

## 🛠️ 实现优先级

### P0 (立即实现)
1. ✅ **会话管理** - 新建/切换/删除会话
2. ✅ **消息操作** - 复制/删除/重发
3. ✅ **权限状态** - 显示无障碍/截图权限状态
4. ✅ **工具执行反馈** - 显示工具执行过程

### P1 (尽快实现)
5. **快捷指令** - 常用操作快捷按钮
6. **工具参数输入** - 完善工具对话框
7. **历史持久化** - 消息保存到数据库
8. **错误详情** - 显示详细错误信息

### P2 (计划实现)
9. **语音输入** - 语音识别
10. **主题切换** - 深色模式
11. **消息搜索** - 搜索历史消息
12. **导出对话** - 导出聊天记录

## 📝 技术实现建议

### 1. 会话管理
```kotlin
// 数据结构
data class Session(
    val id: String,
    val title: String,
    val createdAt: Long,
    val updatedAt: Long,
    val messages: List<Message>
)

// 使用 Room 数据库
@Dao
interface SessionDao {
    @Query("SELECT * FROM sessions ORDER BY updatedAt DESC")
    fun getAll(): Flow<List<Session>>
    
    @Insert
    suspend fun insert(session: Session)
    
    @Delete
    suspend fun delete(session: Session)
}
```

### 2. 消息操作
```kotlin
// 长按菜单
DropdownMenu(
    expanded = showMenu,
    onDismissRequest = { showMenu = false }
) {
    DropdownMenuItem(text = { Text("复制") }, onClick = { /* 复制 */ })
    DropdownMenuItem(text = { Text("重发") }, onClick = { /* 重发 */ })
    DropdownMenuItem(text = { Text("删除") }, onClick = { /* 删除 */ })
}
```

### 3. 工具执行可视化
```kotlin
// 工具执行状态
sealed class ToolExecutionState {
    object Idle : ToolExecutionState()
    data class Executing(val toolName: String, val round: Int, val total: Int) : ToolExecutionState()
    data class Success(val result: String) : ToolExecutionState()
    data class Error(val message: String) : ToolExecutionState()
}

// UI 组件
@Composable
fun ToolExecutionProgress(state: ToolExecutionState) {
    when (state) {
        is ToolExecutionState.Executing -> {
            LinearProgressIndicator()
            Text("正在执行: ${state.toolName} (${state.round}/${state.total})")
        }
        // ...
    }
}
```

### 4. 权限状态
```kotlin
@Composable
fun PermissionStatusPanel() {
    val accessibilityEnabled = remember { mutableStateOf(false) }
    val screenshotGranted = remember { mutableStateOf(false) }
    
    LaunchedEffect(Unit) {
        accessibilityEnabled.value = ChenyiAccessibilityService.getInstance() != null
        screenshotGranted.value = screenshotManager.isAuthorized()
    }
    
    Row {
        StatusChip("无障碍", accessibilityEnabled.value)
        StatusChip("截图", screenshotGranted.value)
    }
}
```

## 🎯 下一步行动

1. **创建 UI 组件库** - 提取可复用组件
2. **实现会话管理** - 添加数据库和 UI
3. **完善消息操作** - 添加长按菜单
4. **改进状态显示** - 权限状态面板
5. **工具执行可视化** - 进度显示组件

## 📊 预期效果

| 功能 | 当前 | 优化后 | 提升 |
|------|------|--------|------|
| 会话管理 | ❌ 无 | ✅ 完整 | +100% |
| 消息操作 | ❌ 无 | ✅ 完整 | +100% |
| 权限状态 | ⚠️ 不清晰 | ✅ 清晰 | +80% |
| 工具反馈 | ❌ 无 | ✅ 可视化 | +100% |
| 用户体验 | ⭐⭐ | ⭐⭐⭐⭐⭐ | +150% |
