# 晨翼Agent 系统流程分析

## 📊 系统架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      用户界面层 (UI)                          │
│  MainActivity.kt - Jetpack Compose UI                        │
│  ├─ 消息列表                                                  │
│  ├─ 输入框                                                    │
│  ├─ 设置对话框                                                │
│  └─ 工具对话框                                                │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│                    Kotlin 桥接层 (JNI)                        │
│  Kernel.kt - JNI 桥接                                         │
│  ├─ chatWithTools() - 主入口                                  │
│  ├─ chat() - 简单聊天                                         │
│  └─ updateConfig() - 配置更新                                 │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│                    Rust 内核层 (Core)                         │
│  AgentKernel - 核心逻辑                                       │
│  ├─ chat() - LLM 调用 + 工具循环                              │
│  ├─ continue_with_tool_results() - 工具结果处理              │
│  ├─ MemoryEngine - 记忆管理                                   │
│  ├─ LlmClient - API 调用                                      │
│  └─ ToolRegistry - 工具注册                                   │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│                  Android 服务层 (Services)                    │
│  ChenyiAccessibilityService - 无障碍服务                      │
│  ├─ executeTool() - 工具执行                                  │
│  ├─ tap/swipe/scroll - 手势操作                               │
│  ├─ screenshot - 截图                                         │
│  ├─ ocr - 文字识别                                            │
│  └─ openApp/closeApp - 应用管理                               │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 用户流程图

### 流程 1: 应用启动

```
用户打开APP
    ↓
MainActivity.onCreate()
    ├─ 初始化 Kernel
    │   └─ nativeInit(dataDir)
    │       └─ Rust: AgentKernel::new()
    │           ├─ 初始化 StorageEngine
    │           ├─ 初始化 MemoryEngine
    │           ├─ 初始化 LlmClient
    │           └─ 初始化 ToolRegistry
    ├─ 初始化 ScreenshotManager
    └─ 初始化 OcrEngine (后台线程)
    ↓
显示主界面
    ├─ 状态栏: "内核已就绪"
    ├─ 消息列表: 空
    └─ 输入框: 等待输入
```

### 流程 2: 用户发送消息

```
用户输入消息 → 点击发送
    ↓
MainActivity: kernel.chatWithTools(message)
    ↓
Kotlin: Kernel.chatWithTools()
    ├─ 第一次调用: chat(message)
    │   └─ nativeChat(message)
    │       └─ Rust: AgentKernel::chat()
    │           ├─ 添加用户消息到历史
    │           ├─ 获取记忆上下文
    │           ├─ 调用 LLM: llm.chat_with_tools()
    │           └─ 检查响应
    │               ├─ 无工具调用 → 返回文本响应
    │               └─ 有工具调用 → 检查回调
    │                   └─ 无回调 → 返回 TOOL_CALLS_REQUIRED
    ↓
Kotlin: 检查返回结果
    ├─ 成功 → 显示响应
    └─ 错误: TOOL_CALLS_REQUIRED
        ↓
    解析工具调用列表
        ↓
    获取 AccessibilityService 实例
        ↓
    循环执行每个工具
        ├─ service.executeTool(name, args)
        │   └─ ChenyiAccessibilityService.executeTool()
        │       ├─ tap/swipe/scroll
        │       ├─ screenshot/ocr
        │       ├─ openApp/closeApp
        │       └─ typeText/pressKey
        └─ 收集工具结果
    ↓
    提交工具结果: nativeSubmitToolResults(results)
        └─ Rust: AgentKernel::continue_with_tool_results()
            ├─ 添加工具结果到历史
            ├─ 再次调用 LLM
            └─ 检查响应
                ├─ 无工具调用 → 返回最终响应
                └─ 有工具调用 → 重复循环（最多10轮）
    ↓
显示最终响应给用户
```

## 🎨 UI 流程图

### 主界面布局

```
┌────────────────────────────────────┐
│  晨翼 Agent ● (状态指示灯)          │  ← TopAppBar
│  [🔧工具] [⚙️设置]                  │
├────────────────────────────────────┤
│  ┌──────────────────────────────┐  │
│  │ 状态栏: "内核已就绪"          │  │  ← Card (状态)
│  └──────────────────────────────┘  │
├────────────────────────────────────┤
│                                    │
│  ┌──────────────────────────────┐  │
│  │ 用户: 帮我打开抖音            │  │  │
│  │ 12:30                        │  │  │
│  └──────────────────────────────┘  │  │
│                                    │  │  ← LazyColumn (消息列表)
│  ┌──────────────────────────────┐  │  │
│  │ 助手: 好的，正在打开抖音...    │  │  │
│  │ 12:30                        │  │  │
│  └──────────────────────────────┘  │  │
│                                    │  │
├────────────────────────────────────┤
│  [输入消息...            ] [➤]    │  ← BottomAppBar (输入框)
└────────────────────────────────────┘
```

### 设置对话框

```
┌────────────────────────────────────┐
│  设置                      [×]    │
├────────────────────────────────────┤
│  API 端点                          │
│  [https://oneapi.xintiandi...]    │
│                                    │
│  API Key                           │
│  [••••••••••••••••••••••••]       │
│                                    │
│  模型名称                          │
│  [glm-5                         ] │
│                                    │
│  当前配置:                         │
│  端点: https://oneapi...          │
│  模型: glm-5                      │
├────────────────────────────────────┤
│         [取消]  [保存]             │
└────────────────────────────────────┘
```

## 🔗 前后端对应关系

### UI 组件 → Kotlin 方法 → Rust 内核

| UI 组件 | Kotlin 方法 | Rust 内核方法 | 功能 |
|---------|------------|--------------|------|
| 发送按钮 | `kernel.chatWithTools()` | `AgentKernel::chat()` | 发送消息并处理工具调用 |
| 设置保存 | `kernel.updateConfig()` | `nativeUpdateConfig()` | 更新 LLM 配置 |
| 清除历史 | `kernel.clearHistory()` | `nativeClearHistory()` | 清除会话历史 |
| 获取状态 | `kernel.getStatus()` | `nativeGetStatus()` | 获取内核状态 |

### 工具执行流程

| Kotlin 层 | Rust 层 | Android 服务层 |
|-----------|---------|---------------|
| `chatWithTools()` | `chat()` | - |
| 解析 TOOL_CALLS_REQUIRED | 返回工具调用列表 | - |
| `service.executeTool()` | - | `tap/swipe/ocr...` |
| `nativeSubmitToolResults()` | `continue_with_tool_results()` | - |
| 显示响应 | 返回最终结果 | - |

## 🐛 发现的流程 Bug

### Bug 1: OCR 初始化时序问题

**问题**: OCR 在后台线程初始化，但用户可能立即发送消息需要 OCR
**位置**: `ChenyiAccessibilityService.kt:44-50`
**影响**: 第一次 OCR 调用可能失败
**修复**: 已添加 `ocrInitialized` 状态检查

### Bug 2: 截图权限未检查

**问题**: 截图工具执行前未检查权限
**位置**: `ChenyiAccessibilityService.kt:107-150`
**影响**: 无权限时截图失败，错误信息不明确
**修复**: 已添加 `hasPermission()` 检查

### Bug 3: 工具执行循环无超时

**问题**: Kotlin 端的 `chatWithTools` 循环没有超时控制
**位置**: `Kernel.kt:99-168`
**影响**: 如果工具调用一直失败，可能无限循环
**修复建议**: 添加总超时时间检查

### Bug 4: 无障碍服务未连接时的处理

**问题**: 无障碍服务未连接时，工具调用会失败
**位置**: `Kernel.kt:122-125`
**影响**: 用户看不到明确的错误提示
**修复建议**: 在 UI 层检查无障碍服务状态

### Bug 5: 配置更新后未重新初始化 LLM 客户端

**问题**: `updateConfig()` 只更新了配置，但 LLM 客户端未重新创建
**位置**: `Kernel.kt:69-74`, `jni.rs:326-388`
**影响**: 配置更新后，需要重启应用才能生效
**修复建议**: 添加 LLM 客户端重新初始化逻辑

### Bug 6: 历史消息未持久化

**问题**: 会话历史只在内存中，应用重启后丢失
**位置**: `AgentKernel::conversation_history`
**影响**: 用户无法查看历史对话
**修复建议**: 添加历史持久化到 SQLite

### Bug 7: 错误信息不够友好

**问题**: Rust 层的错误直接返回给用户，不够友好
**位置**: `Kernel.kt:217-218`
**影响**: 用户看到技术性错误信息
**修复建议**: 添加错误信息翻译层

## 📋 建议的改进

### 高优先级

1. **添加工具执行超时控制** - 防止无限循环
2. **配置更新后重新初始化 LLM** - 让配置立即生效
3. **无障碍服务状态检查** - 提前提示用户

### 中优先级

4. **历史消息持久化** - 保存对话历史
5. **错误信息友好化** - 提升用户体验
6. **添加取消操作功能** - 允许用户中断长时间操作

### 低优先级

7. **添加工具执行进度显示** - 显示当前执行的工具
8. **添加工具执行日志** - 方便调试
9. **支持多会话管理** - 类似 ChatGPT 的会话列表

## 🔧 修复优先级

```
P0 (立即修复):
  ├─ Bug 3: 工具执行循环超时
  ├─ Bug 5: 配置更新后重新初始化
  └─ Bug 4: 无障碍服务状态检查

P1 (尽快修复):
  ├─ Bug 6: 历史消息持久化
  └─ Bug 7: 错误信息友好化

P2 (计划修复):
  ├─ 改进 1: 工具执行进度显示
  └─ 改进 2: 多会话管理
```
